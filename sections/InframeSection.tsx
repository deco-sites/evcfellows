import UIIframeSection, {
  Props,
} from "deco-sites/evcfellows/components/sections/IframeSection.tsx";

function IframeSection(props: Props) {
  return <UIIframeSection {...props} />;
}

export default IframeSection;
